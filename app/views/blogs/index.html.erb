<h1>ブログ一覧</h1>

<p><%= notice %></p>

<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.title %></td>
    <td><%= blog.content %></td>
    <td><%= link_to 'ブログ内容を確認,編集する', edit_blog_path(blog.id) %></td>
  </tr>
<% end %>

</table>


<div></div>
<div></div>

<%= link_to '新しくブログを投稿する', new_blog_path %>